## 1. 变量语义建模

- [x] 1.1 为 description 变量解析链路补充语义分类，至少覆盖 `gold`、`energy`、`cards` 等当前已知高价值单位。
- [x] 1.2 将商店服务、卡牌、遗物、药水描述中的变量来源接入语义分类，优先使用运行时对象成员、动态方法与已绑定的 `LocString` 变量。

## 2. 描述渲染与归一化

- [x] 2.1 在统一描述渲染后处理里实现最小单位补全，只在高置信度场景下把裸数字或图标占位归一化为可读文本。
- [x] 2.2 修复商店移除服务与商店能量类卡牌的 live 输出，确保示例能够分别得到 `增加25金币。` 与 `获得2能量。`

## 3. 验证与诊断

- [x] 3.1 更新 fixture 或验证脚本，覆盖商店服务价格增幅和能量收益描述的断言。
- [x] 3.2 保留无法判定语义时的诊断日志，并完成 `dotnet build mod/Sts2Mod.StateBridge.sln`、`python tools/validate_mod_bridge.py` 与一次 live shop 快照验证。
