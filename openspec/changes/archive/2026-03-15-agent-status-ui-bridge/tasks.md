## 1. HUD 挂载与结构重构

- [x] 1.1 将 `AgentStatusOverlayNode` 重构为稳定的 top-level HUD，并由独立节点负责布局与刷新。
- [x] 1.2 将 HUD 挂载入口收敛到 in-game bridge 入口，避免依赖不稳定的临时可见性调试逻辑。
- [x] 1.3 移除实验性的大色块与临时诊断文件，仅保留必要的节点存在性检查与刷新逻辑。

## 2. 紧凑状态卡片布局

- [x] 2.1 将 HUD 缩减为低遮挡的小尺寸状态卡片，并固定到右上角区域。
- [x] 2.2 使用半透明背景、细边框、统一边距和更紧凑的排版，避免原先大框遮挡问题。
- [x] 2.3 为 `status`、`phase`、动作标签、`confidence`、`turn/step`、`reason` 定义稳定摘要布局与截断规则。

## 3. 字体与文本可读性

- [x] 3.1 参考 `sts2_typing` 引入 sharper fallback font 处理，改善中文渲染和缩放下的可读性。
- [x] 3.2 使用 `RichTextLabel` 承载正文并调整字号与行距，修复空行感和文本溢出问题。
- [x] 3.3 校验 `stale`、`idle`、长文本截断和空字段场景下的卡片布局稳定性。

## 4. Live 验证与文档

- [x] 4.1 完成 live 验证：确认 `/agent-status` 更新后 HUD 可在真实游戏菜单界面稳定显示，并与既有生命周期联调能力兼容。
- [x] 4.2 更新 `README.md` 与 `README.zh.md`，说明 HUD 的用途、位置和调试方式。
- [x] 4.3 清理临时诊断日志与调试代码，只保留后续联调仍需要的最小实现。
